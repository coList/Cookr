{{extend 'layout.html'}}

<h1>
    Neues Rezept erstellen
</h1>
{{=newRecipeForm}}

<h2>
    Zutat hinzufügen
</h2>
{{=addIngredientForm}}

<h2>
   Vorhandene Zutaten
</h2>

<table>
    <thead>
        <tr>
            <td>
                Name
            </td>
            <td>
                Beschreibung
            </td>
        </tr>
    </thead>
    <tfoot>
        {{for row in currentIngredients:}}
        <tr>
            <td>{{=row.name}}</td>
            <td>{{=row.description}}</td>
        </tr>
        {{pass}}
    </tfoot>
</table>
